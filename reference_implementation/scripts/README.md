# Scripts

- `run_minimal_repro.sh` — one-command reproducibility path
- `compare_with_sample.py` — compares a generated pack structurally against the sample pack

Recommended command:

```bash
bash scripts/run_minimal_repro.sh
```
